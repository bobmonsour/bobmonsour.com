<header id="top" class="bg-color">
  <nav>
    <ul class="listolinks">
      {% for link in site.mainNavLinks %}
      <li><a href="{{ link.url }}" {{ link.text | isCurrentPage(page.url) | safe }}>{{ link.text }}</a></li>
      {% endfor %}
    </ul>
  </nav>
</header>
