---
title: Notes
description: These are the archives of Bob Monsour's notes.
keywords: retired, web development, eleventy, tennis, notes
image:
  source: "about-bob.jpg"
  alt: "a picture of Bob from 2009"
permalink: /notes/
---

<h1>{{ title }} <span class="parenthetical">(<a href="/notesfeed.xml">RSS</a>, Notes only)</span></h1>

{%- set noteslist = collections.notes | reverse -%}
{%- for post in noteslist -%}
	<div class="notes post">
		<h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
		<p class="blogdate">{{ post.date | formatPostDate }}</p>
		{% set tagsdata = post.data.tags %}
    {% include 'taglist.njk' %}
		<p class="blogdescription">{{ post.data.description | safe }}</p>
		<hr>
	</div>
{%- endfor -%}
